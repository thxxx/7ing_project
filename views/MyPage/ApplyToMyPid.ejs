<!DOCTYPE html>
<html>

<head>
    <meta charset='utf-8'>
    <title>내 피드에 신청한 사람들 목록</title>
    <script src="/main.js"></script>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="http://code.jquery.com/jquery-latest.min.js"></script>
    <script type="text/javascript" src="/MyInfo.js"></script>
</head>

<body>

    <div>
        <%=user.User_name%>님 안녕하세요</div>
    <button id="logout" onclick="location.href = '/User/logout'">로그아웃</button>
    <button type="button" id="toMainPage" onclick="toMainPage()">메인으로</button>

    <% if (applyToMyPid) {%>
        <% for(let i=0;i<applyToMyPid.length;i++){ %>
            <p>
                <%= applyToMyPid[i].Pid_title %>에
                    <%= applyToMyPid[i].User_name %>님이 신청했습니다.</p>

            <form name="AcceptApply" action="#" method="POST">
                <input type="hidden" id="Pa_code" class="Pa_code" name="Pa_code" value="<%= applyToMyPid[i].Pa_code %>">
                <button class="submit" type="button" onclick="acceptApply()">동행신청 수락</button>
            </form>

            <form name="RejectApply" action="#" method="POST">
                <input type="hidden" id="Pa_code" class="Pa_code" name="Pa_code" value="<%= applyToMyPid[i].Pa_code %>">
                <button class="submit" type="button" onclick="rejectApply()">동행신청 거절</button>
            </form>
            <% } %>
                <% } %>
</body>

</html>