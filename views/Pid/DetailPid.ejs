<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>디테일페이지</title>
    <!-- <script type="module" src="./signupnew.js"></script> -->
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="http://code.jquery.com/jquery-latest.min.js"></script>
    <script type="text/javascript" src="/WritePid.js"></script>
    <script>
        var date = new Date();
    </script>
</head>

<body>
    <div class="container">

        <h2 style="height: 50px; margin-left: 20%; font-size: 2vw; text-align: center; color:#383838;">게시글작성</h2>

        <form name="likeupform" action="#" method="POST">
            <input type="hidden" id="Pid_code" name="Pid_code" class="Pid_code" value="<%= pidcode %>">
            <input type="hidden" id="Pid_good" name="Pid_good" class="Pid_good" value="<%= piddata[0].Pid_good %>">
            <button class="submit" type="button" onclick="likeUp()">좋아요 업</button>
        </form>
        <form name="applypidform" action="#" method="POST">
            <input type="hidden" id="Pid_code" class="Pid_code" name="Pid_code" value="<%= pidcode %>">
            <!-- <button class="submit" type="button">동행신청</button> -->
            <button class="submit" type="button" onclick="applyPid()">동행신청</button>
        </form>

        <div>
            글 작성자 :
            <%= user.User_name %>
                <br> 글작성자 닉네임 :
                <%= user.User_nickname %>
                    <br> 글 작성자 국가:
                    <%= user.User_country  %>
        </div>
        <div>
            타이틀 :
            <%= piddata[0].Pid_title %>
                <br> 내용 :
                <%= piddata[0].Pid_content %>
                    <br> 디데이:
                    <%= 
                    piddata[0].Pid_dday %>

                        <br> 모집인원:
                        <%= piddata[0].Pid_recruitNumber %>
                            <br> 현재인원:
                            <%= piddata[0].Pid_currentNumber %>
                                <br> 좋아요 수:
                                <%= piddata[0].Pid_good %>
                                    <% for(let i=0;i<piddata.length;i++){ %>
                                        <br> 댓글내용:
                                        <%= piddata[i].Pr_content %>
                                            <% } %>
        </div>

        <!-- 작성자 -->
        <p> </p>
        <!-- 작성 일시 -->

        <!-- 내용 -->

        <form name="writeform" action="#" method="POST">
            <!-- 내용 -->
            <input type="textfield" placeholder="본문내용" name="Pid_content" class="Pid_content" maxlength="500"><br /><br />

            <input type="hidden" id="Pid_code" name="Pid_code" class="Pid_code" value="<%= pidcode %>">

            <button class="submit" type="button" onclick="writeReply()">댓글달기<br /><br />
        </form>
        <!-- <button>동행신청하기</button> -->
    </div>
</body>

</html>